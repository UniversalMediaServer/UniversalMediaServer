#----------------------------------------------------------------------------
# Profile for playback inside a web browser that cannot decode lossless audio
# (WebKit / Safari, iOS and iPadOS in particular).
#
# Based on DefaultRenderer.conf
# See DefaultRenderer.conf for a description of all possible configuration options.
#
# Companion to nextcp2webplayer.conf: same web player, but the browser reported
# that it cannot play FLAC, so everything lossless is transcoded to MP3 instead
# of being streamed natively. The nextCP/2 stream proxy picks between the two
# profiles by sending a different User-Agent.
#
# Only audio is handled here; the browser player does not request video or images
# from UMS.
#

#-----------------------------------------------------------------------------
# RENDERER INFORMATION
#

RendererName = NextCP/2 Web Player (lossy)
RendererIcon = nextcp2.svg

#-----------------------------------------------------------------------------
# RENDERER RECOGNITION
#

UserAgentSearch = next_cp_webplayer_lossy

#-----------------------------------------------------------------------------
# BASIC CAPABILITIES
#

# Image is kept enabled so that album-art thumbnails, which the browser fetches
# directly from UMS (also resolving to this profile by IP), are still served.
Video = false
Audio = true
Image = true

#-----------------------------------------------------------------------------
# DLNA SETTINGS
#

# The browser seeks with HTTP byte ranges, not DLNA seek-by-time.
SeekByTime = false

#-----------------------------------------------------------------------------
# MEDIAINFO
#

MediaInfo = true

#-----------------------------------------------------------------------------
# TRANSCODING AND MUXING CAPABILITIES
#

TranscodeAudio = MP3

#-----------------------------------------------------------------------------
# SUPPORTED MEDIA FORMATS
#
# Only what WebKit decodes natively. FLAC, Ogg/Vorbis, Opus and WebM are
# deliberately absent so they fall through to the MP3 transcoder above.
#

Supported = f:mp3|mp2|mpa                                      m:audio/mpeg
Supported = f:m4a               a:aac-lc|he-aac|aac-main       m:audio/mp4
Supported = f:aac|adts          a:aac-lc|he-aac                m:audio/aac
Supported = f:wav               a:adpcm|lpcm                   m:audio/wav
