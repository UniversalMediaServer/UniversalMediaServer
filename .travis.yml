language: java

cache:
  directories:
  - $HOME/.m2

addons:
  apt:
    packages:
    - mediainfo 

matrix:
  include:
    - os: linux
    - os: osx
      osx_image: xcode9.3

install:
    - jdk_switcher use oraclejdk8
    - mvn external:install

script: mvn verify -B -V -P testing
